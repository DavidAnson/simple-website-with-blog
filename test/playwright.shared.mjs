// @ts-check

const domain = "localhost";
export const testPort = 3000;
export const testUrl = `http://${domain}:${testPort}/`;
export const textPort = 3001;
export const textUrl = `http://${domain}:${textPort}/`;
export const photoPort = 3002;
export const photoUrl = `http://${domain}:${photoPort}/`;
